server:
  port: 7777

spring:
  application:
    name: spring-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/durgaaprasadch/lpumicroservice-config.git
          
          clone-on-start: true